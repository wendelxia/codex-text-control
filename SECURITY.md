# 安全策略

## 支持范围

当前只维护 `0.5.x` 源码候选版本。公开仓库中的开发快照不承诺长期安全更新。

## 报告方式

请优先使用 GitHub Private Vulnerability Reporting（GitHub 私密漏洞报告）提交，不要在公开 Issue 中粘贴利用代码、项目上下文、密钥或个人信息。报告入口位于仓库的 `Security` 页面；当前没有单独的安全邮箱。

报告应包含受影响版本、复现步骤、影响范围、是否需要恶意项目文件或用户点击，以及建议修复方向。请删去真实密钥和私人上下文。

## 当前安全边界

- 模型不能直接调用保存和提交工具；写操作只对 Widget 开放。
- 用户正文、版本说明和版本编号按纯文本处理。
- AI 扩展点名称和块内输出在协议层、解析层和存储层校验；块内不能包含起止标记，扩展点外全文由后端从权威基准重建。
- 扩展草稿保存与提交都会核对预期权威版本，检测到中间版本变化时拒绝覆盖。
- Widget 不连接外部域名，内容安全策略中的网络、资源和 iframe（内嵌页面）域名列表为空。
- 存储写入采用临时文件加原子重命名，降低进程中断留下半个 JSON 文件的概率。
- 同一权威状态的后继转换使用排他硬链接发布；两个 MCP 进程从同一基准更新时最多一个转换能成为事实，旧指针晚写也不会让权威内容倒退。
- 插件在用户选择的项目目录中写 `.codex-text-control/`；它没有网络上传逻辑。

## 已知风险

- 插件没有对项目目录做沙箱限制；获得 Widget 写权限的宿主可指定其他可写目录。
- 本地文件没有加密，能读取项目目录的本机进程也能读取上下文。
- 超时只表示客户端没收到结果，不能证明服务端没有完成写入。
- 当前没有独立渗透测试，也没有单独的安全邮箱；私密漏洞报告是公开安全联系入口。
- 状态转换依赖本地文件系统的原子硬链接和重命名语义，不是跨机器分布式事务；网络文件系统和不支持硬链接的文件系统尚未验证。

## 客观评价

最小权限、无外网和纯文本渲染显著缩小了攻击面，但“npm audit 为 0”只代表官方漏洞库暂未收录已安装依赖的问题，不等于软件没有未知漏洞。
