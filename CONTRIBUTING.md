# 贡献指南

本项目欢迎修复缺陷、补测试和改善文档。当前阶段优先保证“编辑 -> 保存修订 -> 用户提交 -> 对话收到权威消息 -> 重开仍能读取”这条核心闭环，不接受用大量外围功能掩盖闭环问题。

## 开发环境

- Node.js（JavaScript 运行时）22 或更高版本。
- npm（Node 包管理器），使用仓库里的 `package-lock.json` 锁定依赖。
- Windows、macOS 或 Linux；真实 Codex Widget 验收还需要支持 MCP Apps（MCP 应用）的 Codex 桌面宿主。

## 本地检查

```powershell
npm ci --ignore-scripts
npm run quality
npm run verify:candidate
```

`npm ci --ignore-scripts` 从锁文件干净安装依赖并阻止依赖安装脚本；`quality` 跑测试、语法检查和 MCP 探测；`verify:candidate` 还会连接 npm 官方注册表检查已知漏洞、包签名和来源证明。它只证明候选检查通过，不代表项目已经达到发布等级。

## 变更要求

1. 行为修复先增加能复现问题的测试，再改实现。
2. 写操作必须保持 `app-only`，不能让模型绕过用户确认。
3. 新功能必须说明它如何改善核心闭环，以及为什么现有代码不能复用。
4. 用户文本一律当作不可信纯文本，不能写入 `innerHTML`。
5. 修改版本格式、指针格式或权限边界时，要在 `docs/design/architecture.md` 增加架构决策说明。
6. PR（拉取请求）必须写明已证实、未证实、失败项、限制和回退方法。

## 提交前人工验收

在真实 Codex 中打开编辑器，至少检查保存、提交、消息回传、重开恢复、超时提示、窄屏和键盘操作。自动测试只能证明受控场景，不能代替真实宿主。

## 客观评价

这套贡献门槛会让小改动多花几分钟，但能阻止“构建能过就算完成”的问题。当前仍缺公开 Issue 模板、发布签名和多操作系统真实宿主矩阵，这些应在首次公开 Release 前补齐。
